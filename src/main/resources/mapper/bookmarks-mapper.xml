<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.plugin.bookmarks.model.dao.BookmarksMapper">

	<select id="findBookmark">
		SELECT
		       BOOKMARK_NO
		     , USER_ID
		     , STATION_NO
		     , CREATE_DATE
		  FROM
		       BOOKMARKS
		 WHERE
		       USER_ID = #{userId}
		   AND
		       STATION_NO = #{stationNo}                
	</select>

	<insert id="saveBookmark">
		INSERT
		  INTO
		       BOOKMARKS
		VALUES
		       (
			   SEQ_BMN.NEXTVAL
			 , #{userId}
			 , #{stationNo}
			 , SYSTIMESTAMP  	
			   )         
	</insert>
	
	<delete id="deleteBookmark">
		DELETE
		  FROM
		       BOOKMARKS
		 WHERE
		       BOOKMARK_NO = #{bookmarkNo}      
	</delete>

	<select id="findBookmarksList">
		SELECT
		       BOOKMARK_NO
		     , USER_ID
		     , STATION_NO
		     , CREATE_DATE
		  FROM
		       BOOKMARKS
		 WHERE
		       USER_ID = #{userId}
		 ORDER
		    BY
		       CREATE_DATE DESC                 
	</select>

</mapper>